## 中文

v0.1.1 - 硅基流动支持与自定义 API

### ✨ 新功能
- **硅基流动转录**: 新增国内 SiliconFlow API 支持，70ms 超低延迟
- **自定义 API 端点**: 支持配置自定义 base_url，可用于代理或兼容服务
- **图标显示优化**: 修复应用图标在任务栏和窗口标题栏的显示问题

### 🐛 修复
- 修复 SiliconFlowEngine 缺少 device 属性的错误
- 修复 SiliconFlowEngine.transcribe() temperature 参数兼容性问题
- 修复 log_error 调用的参数类型错误
- 改进 load_model() 和 test_connection() 使用真实转录请求验证 API
- 改善非 exe 模式下的图标显示

### 🔧 系统要求
- Windows 10/11 (64-bit)
- 需要网络连接

### 📖 使用说明

**硅基流动配置示例**:
```json
{
  "speech": {
    "provider": "siliconflow",
    "siliconflow": {
      "api_key": "your-api-key-here",
      "model": "FunAudioLLM/SenseVoiceSmall"
    }
  }
}
```

**自定义 API 端点** (可选):
```json
{
  "speech": {
    "siliconflow": {
      "base_url": "https://your-proxy-url.com/v1"
    }
  }
}
```

---

## English

v0.1.1 - SiliconFlow Support & Custom API

### ✨ New Features
- **SiliconFlow Transcription**: Added SiliconFlow API support with 70ms ultra-low latency
- **Custom API Endpoint**: Support configurable base_url for proxy or compatible services
- **Icon Display Fix**: Fixed app icon display in taskbar and window title bar

### 🐛 Bug Fixes
- Fixed missing device attribute in SiliconFlowEngine
- Fixed SiliconFlowEngine.transcribe() temperature parameter compatibility
- Fixed log_error parameter type issues
- Improved load_model() and test_connection() to use real transcription requests for API validation
- Improved icon display in non-exe mode

### 🔧 System Requirements
- Windows 10/11 (64-bit)
- Internet connection required

### 📖 Usage

**SiliconFlow Configuration**:
```json
{
  "speech": {
    "provider": "siliconflow",
    "siliconflow": {
      "api_key": "your-api-key-here",
      "model": "FunAudioLLM/SenseVoiceSmall"
    }
  }
}
```

**Custom API Endpoint** (optional):
```json
{
  "speech": {
    "siliconflow": {
      "base_url": "https://your-proxy-url.com/v1"
    }
  }
}
```
